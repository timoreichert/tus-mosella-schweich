import { FirebaseCrashlyticsErrorHandler } from './firebase-crashlytics-error-handler';

describe('FirebaseCrashlyticsErrorHandler', () => {
  it('should create an instance', () => {
    expect(new FirebaseCrashlyticsErrorHandler()).toBeTruthy();
  });
});
